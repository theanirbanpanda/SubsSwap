import React from "react";

const VerifiedBadge = () => (
  <span style={{
    backgroundColor: "#4CAF50",
    color: "#fff",
    padding: "0.2rem 0.5rem",
    borderRadius: "10px",
    fontSize: "0.8rem",
    marginLeft: "0.5rem"
  }}>
    Verified
  </span>
);

export default VerifiedBadge;
